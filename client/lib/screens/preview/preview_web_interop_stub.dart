// Non-web stub for preview_web_interop.dart.
// Provides the same public API with no-op / empty implementations so that
// dart.library.io platforms (Android, iOS, desktop) compile without error.

/// Non-web stub — returns empty string.
String previewCreateBlobUrl(List<int> bytes, String mimeType) => '';

/// Non-web stub — no-op.
void previewRevokeBlobUrl(String url) {}
